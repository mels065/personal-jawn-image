sudo: required

language: ruby

services:
  - docker
  
before_install:
  - docker build -t mellusb/jawn-dev
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push